# Pinned with version floors; exact versions are frozen in requirements.lock
# after a successful install (`make lock`).
#
# NOTE on upper bounds: econml and shap need numpy<2 and an older
# scikit-learn at time of writing. These bounds are deliberate, not stale.

# --- Core ---
pandas>=2.0
numpy>=1.26,<2.0
scipy>=1.11
scikit-learn>=1.3,<1.6

# --- Predictive models ---
statsmodels>=0.14
xgboost>=2.0
lightgbm>=4.0

# --- Interpretation & fairness ---
shap>=0.44
fairlearn>=0.10

# --- Causal / uplift ---
econml>=0.15
dowhy>=0.11

# --- Visualisation ---
matplotlib>=3.7
seaborn>=0.13

# --- Notebooks & tests ---
jupyter>=1.0
pytest>=7.4
